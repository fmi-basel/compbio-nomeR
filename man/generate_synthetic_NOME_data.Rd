% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nomeR-package.R
\name{generate_synthetic_NOME_data}
\alias{generate_synthetic_NOME_data}
\title{Generate synthetic NOME-Seq like dataset}
\usage{
generate_synthetic_NOME_data(
  amplicon_len,
  n_reads,
  footprint_models,
  bgprotectprob,
  GpC_param = 1,
  extend_ampl = FALSE
)
}
\arguments{
\item{amplicon_len}{length of a synthetic amplicon}

\item{n_reads}{number of reads to generate}

\item{footprint_models}{footprint models}

\item{bgprotectprob}{background emission probabilitity for 1}

\item{GpC_param}{if scalar between 0 and 1 treated as percentage of GpC pos. If vector of integers treated as predefined positions of GpC.}

\item{extend_ampl}{whether to extend amplicon by \code{max(length(footprint_models))} and fill it with \code{NA}.}
}
\value{
TODO
}
\description{
Generate synthetic NOME-Seq like dataset
}
\examples{

\dontrun{

nucl_cover_prior <- 0.3
nucl_len <- 10
nucl_prot_prob <- 0.99

tf_cover_prior <- 0.05
tf_len <- 5
tf_prot_prob <- 0.99


bg_prot_prob <- 0.01

footprint_models <- list(list("PROTECT_PROB" = rep(nucl_prot_prob,nucl_len),
                              "COVER_PRIOR" = nucl_cover_prior,
                              "NAME" = "Nucleosome"),
                         list("PROTECT_PROB" = rep(tf_prot_prob,tf_len),
                              "COVER_PRIOR" = tf_cover_prior,
                              "NAME" = "TF"))

#### simulate nome data
amp.len <- 500
n.reads <- 5000


set.seed(338485)
synthetic_nome_data <- generate_synthetic_NOME_data(amplicon_len = amp.len,
                                                  n_reads = n.reads,
                                                  footprint_models = footprint_models,
                                                  bgprotectprob = bg_prot_prob)

}

}

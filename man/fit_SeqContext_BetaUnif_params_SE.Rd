% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_SeqContext_BetaUnif_params_SE.R
\name{fit_SeqContext_BetaUnif_params_SE}
\alias{fit_SeqContext_BetaUnif_params_SE}
\title{Fit Beta-Uniform mixture models for positive and negative controls}
\usage{
fit_SeqContext_BetaUnif_params_SE(
  se,
  neg_control_sampleName,
  pos_control_sampleName,
  assayName = "mod_prob",
  min_obs = 100L,
  eps_max_pos = 0.15,
  eps_max_neg = 0.25,
  ncpu = 1L
)
}
\arguments{
\item{se}{A \code{\link[SummarizedExperiment]{SummarizedExperiment}} object
containing read-level data, such as that returned by
\code{SingleMoleculeGenomicsIO::readModBam}, including modification probabilities.}

\item{neg_control_sampleName}{Character scalar. Sample name for the negative
control (SMF experiment without MTase treatment).}

\item{pos_control_sampleName}{Character scalar. Sample name for the positive
control (SMF experiment with MTase treatment on naked DNA).}

\item{assayName}{Character scalar specifying the name of the assay in
\code{se} that contains read-level modification probabilities in
\code{[0, 1]}, where high values indicate accessible (methylated) positions.}

\item{min_obs}{Integer. Minimum number of finite observations required in
\strong{both} controls for a context to receive its own model. Default: \code{100}.}

\item{eps_max_pos}{Numeric. Maximum uniform weight for the positive control
mixture. Default: \code{0.15}.}

\item{eps_max_neg}{Numeric. Maximum uniform weight for the negative control
mixture. Default: \code{0.25}. Higher values prevent the negative-control
density from collapsing to zero near the accessible end.}

\item{ncpu}{Integer. Number of parallel workers for context fitting via
\code{parallel::mclapply} (Unix/macOS only). Default: \code{1L}.}
}
\value{
A \code{data.table} with one row per fitted sequence context and
columns:
\describe{
\item{seqcont}{Context label (\code{"OTHER"} for pooled sparse contexts).}
\item{alpha_pos, beta_pos, eps_pos}{Beta-Uniform parameters for the positive control.}
\item{alpha_neg, beta_neg, eps_neg}{Beta-Uniform parameters for the negative control.}
\item{n_pos, n_neg}{Number of observations used for fitting.}
\item{converged}{Logical: did the EM converge?}
\item{mlrp_ok}{Logical: is the MLRP constraint satisfied after fitting?}
}
}
\description{
Fits per-context Beta-Uniform mixture parameters using joint constrained EM
(Expectation-Maximisation + Newton-Raphson), with a monotone-likelihood-
ratio-preserving (MLRP) projection at each step. The constraint
\code{alpha_pos >= alpha_neg} and \code{beta_pos <= beta_neg} guarantees
that the resulting posterior correction is monotone in the raw probability.
}
\details{
Sequence contexts with fewer than \code{min_obs} finite observations in
\strong{either} control are pooled into a single \code{"OTHER"} category before
fitting.
}
